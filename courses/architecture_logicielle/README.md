# Architecture logicielle

Compilation faite avec MikTex.  
Il peut être nécessaire d'installer manuellement (via le package-manager) les packages `beamer` et `translator`.  
La génération de la **Table Of Content** nécessite de compiler deux fois.

Pour compiler : `pdflatex presentation_1.pdf`
